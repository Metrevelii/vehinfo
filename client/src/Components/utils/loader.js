import React from 'react';
// import CircularProgress from '@mui/material/CircularProgress';

const Loader = ({ full }) => (
    <div className={`mt-[50%] lg:mt-[30%] xl:mt-[30%] 2xl:mt-[30%] flex justify-center items-center font-interMedium font-700 text-primary-black text-[20px]`}>
        <p>Loading ...</p>
    </div>
)

export default Loader;